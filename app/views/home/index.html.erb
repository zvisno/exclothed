<div class="container">

  <div class="column col-xs-4">
    <% @user_items.each do |key, value|%>
        <div class="item">
          <%= image_tag('default')%>
          <p><%= value[0]%></p>
          <p><%= value[1] %></p>
          <p><%= value[2]%></p>
        </div>
    <%end%>

  </div>

  <div class="column col-xs-4"></div>
</div>
